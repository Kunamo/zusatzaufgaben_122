#!/bin/bash
#get all files ending with .jpg
#allFiles=$(find . -name "*.jpg" |
date=$(date +%Y-%m-%d)
count=$(ls | wc -l)

renFiles () {
  mv
}

for file in count; do
  renFiles
done

#files=("anotherfile" "somefile" "somedir")
#for file in "${files[@]}"; do #@ -> is for looping trough ALL of the array
 # echo "result of $file: "
  #test -e "$file" | file "$file" && ls -l "$file"
#done
